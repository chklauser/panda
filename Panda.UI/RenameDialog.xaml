<Window x:Class="Panda.UI.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="clr-namespace:Panda.UI"
        Title="RenameDialog" Height="300" Width="300">
    <Grid>
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="4">
            <TextBox Name="NodeName" Grid.Column="1" Grid.Row="0">
                <TextBox.Text>
                    <Binding Path="Left" UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <ui:NodeNameValidationRule />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
            <Button Name="OkButton" Click="okButton_Click" IsDefault="True">OK</Button>
            <Button Name="CancelButton" Click="cancelButton_Click"  IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
